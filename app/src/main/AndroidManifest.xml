<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hari.learningfirebase">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.LearningFirebase"
    android:requestLegacyExternalStorage="true">
    <activity android:name=".storage.IgMiniActivity">
    </activity>
    <activity android:name=".firestore.RegisterScActivity">
    </activity>
    <activity android:name=".firestore.DashBoardScActivity">
    </activity>
    <activity android:name=".realtime.LeaderBoardActivity">
    </activity>
    <activity android:name=".auth.ForgotPasswordActivity">
    </activity>
    <activity android:name=".auth.LoginHpActivity">
    </activity>
    <activity android:name=".auth.RegisterEmailActivity">
    </activity>
    <activity android:name=".auth.LoginEmailActivity">
    </activity>
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>